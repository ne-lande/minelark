<!-- Generated by `./gradlew generateApiDocs` from @StarlarkMethod annotations. Do not edit by hand. -->

# Loot API Reference

The **`loot`** namespace, available to scripts in the **`minelark/server/`** folder.
Replace what an entity drops, or inject extra drops into an existing loot table.
Each drop is an item id, or a `{"item": id, "count": N or [min, max], "chance":
0.0-1.0}` dict. Reloadable with `/minelark reload`.

---

## `loot.entity_drops(entity, drops)`

Replaces what an entity drops when killed. Each drop is an item id or a `{"item", "count", "chance"}` dict. This overrides the entity's vanilla drops.

**Parameters**

| Name | Type | Required | Default | Description |
|---|---|---|---|---|
| `entity` | string | yes | - | The entity type id, e.g. `"minecraft:zombie"`. |
| `drops` | any | yes | - | A list of drops (item ids or dicts). |

---

## `loot.inject(table, drops)`

Adds extra drops to an existing loot table (e.g. a chest) without replacing it. Each drop is an item id or a `{"item", "count", "chance"}` dict.

**Parameters**

| Name | Type | Required | Default | Description |
|---|---|---|---|---|
| `table` | string | yes | - | The loot table id, e.g. `"minecraft:chests/simple_dungeon"`. |
| `drops` | any | yes | - | A list of drops (item ids or dicts) to add as one pool. |

---

*The usual Starlark builtins (`print`, `len`, `range`, and so on) work here too. See [The Starlark Language](../language.md).*
